import React from 'react';
import logo from "../images/logo.svg";

export default (() => {
    return (
        <nav>
            <img src={logo} alt="logo" />
            my travel journal
        </nav>
    )
});